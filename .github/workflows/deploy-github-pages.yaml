# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v3

      - id: parsing
        run: |
          echo "files=$(yq -o json '.[0].repos' files.yaml | jq . --compact-output)" | tee -a $GITHUB_OUTPUT

      - uses: actions/github-script@v6
        with:
          script: |
            const owner = '${{ github.repository_owner }}'
            const repo = 'test-repo-file-observer-0'
            const path = '.github/workflows/random.yaml'
            const contents = await github.rest.repos.getContent({
              owner,
              repo,
              path,
            });
            console.log(contents);

            const arr = ${{ steps.parsing.outputs.files }}
            console.log(arr);

      - uses: actions/configure-pages@v3

      - name: Upload directory of site, _site/
        uses: actions/upload-pages-artifact@v1

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v1
